[package]
name = "test-jwt"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
josekit.workspace = true

verder-helpen-common = { path = "../../verder-helpen-common", features = [
    "jwt",
] }
